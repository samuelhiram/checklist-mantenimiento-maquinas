import { RouteLoadingScreen } from '@/components/feedback/RouteLoadingScreen'

export default function Loading() {
  return (
    <RouteLoadingScreen
      variant="workspace"
      title="Cargando administracion"
      description="Preparando modulos, accesos y resumen operativo del panel administrativo."
    />
  )
}
