import { RouteLoadingScreen } from '@/components/feedback/RouteLoadingScreen'

export default function Loading() {
  return (
    <RouteLoadingScreen
      variant="editor"
      title="Preparando alta de maquina"
      description="Montando formulario, acciones y validaciones iniciales para la nueva maquina."
    />
  )
}
